<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update user</title>
    <link
    href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
    rel="stylesheet"
  />
     <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
  </head>
  <style>
    .stbtn{
      float: right;
      margin-top: -50px;
    }
  </style>
  <body>
      <div class="container mt-4">
        <h1>Update User</h1>
        <div> 
        <% if(users.isVery === 2) {%>
          <button class="btn btn-danger btn-sm stbtn update-status" data-userid="<%= users._id %>" data-status = "3">Suspend</button>
        <% } else if(users.isVery === 1 || users.isVery === 3 ) {%>
          <button class="btn btn-success btn-sm stbtn update-status" data-userid="<%= users._id %>" data-status="2">Active</button>
        <% } %>
       </div>
        
        <form id="updateinfo" enctype="multipart/form-data">
          <label for="image">Upload Images:</label> <br>  

          <% if(users.img && users.img !== undefined && users.img.length > 0){ %>
          <img
            src="<%= users.img%>"
            alt="current profile"
            class="profile-img"
            width="80"
          />
          <% } else { %>
          <img
             src="http://localhost:3002/img/Profile.png"
            alt="Profile"
            class="profile-img"
            width="80"
          />
          <% } %> 
              <input
                      type="file"
                      id="profilePreview" 
                      name="user_images"
                      multiple
                      />           
          </div>       

  <label>Username :</label>
  <input type="text" minlength="3" maxlength="16" name="username" class="form-control" value="<%= users.username %>" placeholder="Enter Username" /><br>

  <label>Mobile No :</label>
  <input type="no" name="mobileno" maxlength="10" class="form-control" value="<%= users.mobileno %>" placeholder="Enter Mobile No" /><br>

  <label>Email :</label>
  <input type="email" name="email" class="form-control" value="<%= users.email %>" placeholder="Enter Email" disabled/><br>

  <label>Gender:</label>
  <select name="gender" class="form-control">
    <option value="">Select Gender</option>
    <option value="male" <%= users.gender === 'male' ? 'selected' : '' %>>Male</option>
    <option value="female" <%= users.gender === 'female' ? 'selected' : '' %>>Female</option>
    <option value="other" <%= users.gender === 'other' ? 'selected' : '' %>>Other</option>
  </select><br>

  <input type="hidden" name="userId" value="<%= users._id %>">

  <button type="submit" class="btn btn-primary custom-btn">Update User</button>
</form>

</div>
  </body>
</html>
